# TorchTPU Documentation

This directory holds the source code for TorchTPU documentation. You should not
try to run the Python code in this directory directly. Instead, TorchTPU CI will
run these scripts to generate the documentation HTML files.
